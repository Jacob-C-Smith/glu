{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch QEMU",
            "type": "shell",
            "linux": {
                "command": "./build.sh; qemu-system-i386 -S -gdb tcp::1234 -boot d -cdrom glu.iso -m 512 -daemonize"
            }
        }
    ]
}